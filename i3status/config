# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
        colors = true
        interval = 5
}

# order += "ipv6"
# order += "read_file current_window_layout"
order += "wireless _first_"
order += "ethernet _first_"
order += "battery all"
order += "disk /"
order += "load"
order += "cpu_usage"
order += "cpu_temperature 0"
order += "memory"
order += "volume master"
order += "tztime local"
# order += "read_file display_brightness"

wireless _first_ {
        format_up = "󰖩 %essid %ip(%quality)"
        format_down = "󱚼 down"
}

ethernet _first_ {
        format_up = "󰱓 %ip (󰓅 %speed)"
        format_down = "󰅛 down"
}

battery all {
        format = "%status %percentage %remaining"
        format_down = " %percentage "
        status_chr = "󱐋"
        status_bat = " "
        status_full = " "
	status_idle = ""
	low_threshold = 60
	threshold_type = time
}

disk "/" {
        format = " %avail"
}

load {
        format = "%1min"
	max_threshold = 12
}

cpu_usage {
	format = " %usage "
}

memory {
        format = " %used/%total"
        threshold_degraded = "1G"
        format_degraded = "MEMORY < %available"
}

tztime local {
        format = "󰸗 %Y/%m/%d 󱑁 %H:%M"
}

volume master {
	format = " %volume"
	format_muted = ""
}

cpu_temperature 0 {
	format = " %degrees°C"
	path = "/sys/class/hwmon/hwmon6/temp1_input"
}

read_file display_brightness {
	path = "/sys/class/backlight/amdgpu_bl1/brightness"
	format = "󰃟 %content/255"
}
